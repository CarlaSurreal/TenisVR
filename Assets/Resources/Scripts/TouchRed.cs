using UnityEngine;

public class TouchRed : MonoBehaviour
{
    public bool touchedRed = false; // Start is called once before the first execution of Update after the MonoBehaviour is created
    public bool touchedFloor = false;
}
